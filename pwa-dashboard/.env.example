# Copy to .env.local — never commit .env.local

# FastAPI backend used by the dashboard's read path (src/api/*).
VITE_API_URL=http://localhost:8000

# pwa-counter, linked from the Hero "Jouer maintenant" CTA.
VITE_COUNTER_URL=http://localhost:5175
